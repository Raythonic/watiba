#!/usr/bin/python3.8
import os
import re

`cd /mnt/data3`
print(`echo "BLAH!" > /tmp/blah.txt && tar -zcvf /tmp/blah.tar.gz /tmp/blah`.stderr)
print(`echo "hello!"`.stdout[0])
xc = `lsvv -lrt`.exit_code

print("Return code: {}".format(xc))